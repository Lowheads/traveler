<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.rog//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.travelmaker.mapper.ThemeMapper">

<select id="getThemeList" resultType="map">
	select * from theme
</select>

<select id="getThemeInfo" resultType="map">
	SELECT T.PLC_NO, P.PLC_TITLE 
	FROM THEME_PLC T, PLACE P 
	WHERE T.PLC_NO = P.PLC_NO AND THEME_NO = (
		SELECT THEME_NO 
		FROM THEME 
		WHERE REGION_NO=#{regionNo} AND THEME_CODE=#{themeCode})

</select>
</mapper>